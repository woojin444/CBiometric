cmd codes:
pip install numpy
pip install scipy
pip install opencv_python
pip install --upgrade tensorflow
pip install -U scikit-learn
pip install Pillow
pip install reportlab


After installing all the cmd codes: 
Add facenet-master/src to environment variable
