Install only first time:
Install node from https://nodejs.org/en/download/
Run CMD:
	npm install -g concurrently
	npm install -g nodemon

CD into folder web_app:
	npm install
CD into web_app/client:
	npm install
cd into web_app
	npm run dev
